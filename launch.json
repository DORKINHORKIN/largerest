{
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Launch index.html",
      "file": "${workspaceFolder}/${input:htmlFile}"
    }
  ],
  "inputs": [
    {
      "type": "promptString",
      "id": "htmlFile",
      "description": "Enter the path to the HTML file to debug (e.g., index.html)"
    }
  ]
}